import React from "react";
import Counter from "../components/Counter";
import Layout from "../components/Layout";

const Auth = () => {
  return (
    <Layout>
      <Counter />
    </Layout>
  );
};

export default Auth;
